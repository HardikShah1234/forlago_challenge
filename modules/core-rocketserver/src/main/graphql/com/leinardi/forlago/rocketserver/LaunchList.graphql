query LaunchList {
    launchConnection: launches {
        launches {
            id
            site
            mission {
                name
                missionPatch(size: SMALL)
            }
        }
    }
}
